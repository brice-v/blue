import gg
# gg contains the Rectangle function to attempt to parse, but the other file
# does not actually import gg so it cant be compiled
# Note: this still fails due to gg not being imported properly yet
assert(true);