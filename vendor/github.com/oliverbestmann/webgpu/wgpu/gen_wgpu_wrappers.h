// Code generated by cmd/cgohints: DO NOT EDIT.

#pragma once

#include <stdlib.h>
#include <string.h>
#include <wgpu.h>

static inline size_t error_buf_size() { return 16 * 1024; }

static inline void webgpu_error_callback(WGPUPopErrorScopeStatus status,
                                         WGPUErrorType type,
                                         WGPUStringView message,
                                         WGPU_NULLABLE void *userdata1,
                                         WGPU_NULLABLE void *userdata2) {
  char *err = (char *)userdata1;
  if (type == WGPUErrorType_NoError) {
    return;
  }

  // handle the case that we get a null data pointer
  if (message.data == NULL) {
    message.data = "unknown error";
    message.length = WGPU_STRLEN;
  }

  // update length if not specified
  if (message.length == WGPU_STRLEN) {
    message.length = strlen(message.data);
  }

  // cap message length to available buffer size
  size_t buf_size = error_buf_size();
  if (message.length > buf_size - 1) {
    message.length = buf_size - 1;
  }

  // now we can just copy the data over
  memcpy(err, message.data, message.length);

  // and set the zero byte at the end
  err[message.length] = 0;
}

static inline WGPUInstance
go_wgpuCreateInstance(WGPUDevice _dev, char *err,
                      WGPU_NULLABLE WGPUInstanceDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUInstance ret = wgpuCreateInstance(descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void
go_wgpuGetInstanceFeatures(WGPUDevice _dev, char *err,
                           WGPUSupportedInstanceFeatures *features) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuGetInstanceFeatures(features);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus go_wgpuGetInstanceLimits(WGPUDevice _dev, char *err,
                                                  WGPUInstanceLimits *limits) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuGetInstanceLimits(limits);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBool
go_wgpuHasInstanceFeature(WGPUDevice _dev, char *err,
                          WGPUInstanceFeatureName feature) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBool ret = wgpuHasInstanceFeature(feature);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUProc go_wgpuGetProcAddress(WGPUDevice _dev, char *err,
                                             WGPUStringView procName) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUProc ret = wgpuGetProcAddress(procName);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuAdapterGetFeatures(WGPUDevice _dev, char *err,
                                             WGPUAdapter adapter,
                                             WGPUSupportedFeatures *features) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuAdapterGetFeatures(adapter, features);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus go_wgpuAdapterGetInfo(WGPUDevice _dev, char *err,
                                               WGPUAdapter adapter,
                                               WGPUAdapterInfo *info) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuAdapterGetInfo(adapter, info);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUStatus go_wgpuAdapterGetLimits(WGPUDevice _dev, char *err,
                                                 WGPUAdapter adapter,
                                                 WGPULimits *limits) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuAdapterGetLimits(adapter, limits);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBool go_wgpuAdapterHasFeature(WGPUDevice _dev, char *err,
                                                WGPUAdapter adapter,
                                                WGPUFeatureName feature) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBool ret = wgpuAdapterHasFeature(adapter, feature);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture go_wgpuAdapterRequestDevice(
    WGPUDevice _dev, char *err, WGPUAdapter adapter,
    WGPU_NULLABLE WGPUDeviceDescriptor const *descriptor,
    WGPURequestDeviceCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuAdapterRequestDevice(adapter, descriptor, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuAdapterAddRef(WGPUDevice _dev, char *err,
                                        WGPUAdapter adapter) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuAdapterAddRef(adapter);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuAdapterRelease(WGPUDevice _dev, char *err,
                                         WGPUAdapter adapter) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuAdapterRelease(adapter);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuAdapterInfoFreeMembers(WGPUDevice _dev, char *err,
                                                 WGPUAdapterInfo adapterInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuAdapterInfoFreeMembers(adapterInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBindGroupSetLabel(WGPUDevice _dev, char *err,
                                            WGPUBindGroup bindGroup,
                                            WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupSetLabel(bindGroup, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBindGroupAddRef(WGPUDevice _dev, char *err,
                                          WGPUBindGroup bindGroup) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupAddRef(bindGroup);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBindGroupRelease(WGPUDevice _dev, char *err,
                                           WGPUBindGroup bindGroup) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupRelease(bindGroup);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuBindGroupLayoutSetLabel(WGPUDevice _dev, char *err,
                               WGPUBindGroupLayout bindGroupLayout,
                               WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupLayoutSetLabel(bindGroupLayout, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuBindGroupLayoutAddRef(WGPUDevice _dev, char *err,
                             WGPUBindGroupLayout bindGroupLayout) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupLayoutAddRef(bindGroupLayout);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuBindGroupLayoutRelease(WGPUDevice _dev, char *err,
                              WGPUBindGroupLayout bindGroupLayout) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBindGroupLayoutRelease(bindGroupLayout);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBufferDestroy(WGPUDevice _dev, char *err,
                                        WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBufferDestroy(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUBufferMapState
go_wgpuBufferGetMapState(WGPUDevice _dev, char *err, WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBufferMapState ret = wgpuBufferGetMapState(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline uint64_t go_wgpuBufferGetSize(WGPUDevice _dev, char *err,
                                            WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint64_t ret = wgpuBufferGetSize(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBufferUsage go_wgpuBufferGetUsage(WGPUDevice _dev, char *err,
                                                    WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBufferUsage ret = wgpuBufferGetUsage(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture
go_wgpuBufferMapAsync(WGPUDevice _dev, char *err, WGPUBuffer buffer,
                      WGPUMapMode mode, size_t offset, size_t size,
                      WGPUBufferMapCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuBufferMapAsync(buffer, mode, offset, size, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUStatus
go_wgpuBufferReadMappedRange(WGPUDevice _dev, char *err, WGPUBuffer buffer,
                             size_t offset, void *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuBufferReadMappedRange(buffer, offset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuBufferSetLabel(WGPUDevice _dev, char *err,
                                         WGPUBuffer buffer,
                                         WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBufferSetLabel(buffer, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBufferUnmap(WGPUDevice _dev, char *err,
                                      WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBufferUnmap(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus
go_wgpuBufferWriteMappedRange(WGPUDevice _dev, char *err, WGPUBuffer buffer,
                              size_t offset, void const *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuBufferWriteMappedRange(buffer, offset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuBufferAddRef(WGPUDevice _dev, char *err,
                                       WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBufferAddRef(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuBufferRelease(WGPUDevice _dev, char *err,
                                        WGPUBuffer buffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuBufferRelease(buffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandBufferSetLabel(WGPUDevice _dev, char *err,
                                                WGPUCommandBuffer commandBuffer,
                                                WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandBufferSetLabel(commandBuffer, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandBufferAddRef(WGPUDevice _dev, char *err,
                                              WGPUCommandBuffer commandBuffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandBufferAddRef(commandBuffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandBufferRelease(WGPUDevice _dev, char *err,
                            WGPUCommandBuffer commandBuffer) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandBufferRelease(commandBuffer);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUComputePassEncoder go_wgpuCommandEncoderBeginComputePass(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPU_NULLABLE WGPUComputePassDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUComputePassEncoder ret =
      wgpuCommandEncoderBeginComputePass(commandEncoder, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPURenderPassEncoder go_wgpuCommandEncoderBeginRenderPass(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPURenderPassDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPURenderPassEncoder ret =
      wgpuCommandEncoderBeginRenderPass(commandEncoder, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuCommandEncoderClearBuffer(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUBuffer buffer, uint64_t offset, uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderClearBuffer(commandEncoder, buffer, offset, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderCopyBufferToBuffer(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUBuffer source, uint64_t sourceOffset, WGPUBuffer destination,
    uint64_t destinationOffset, uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderCopyBufferToBuffer(commandEncoder, source, sourceOffset,
                                       destination, destinationOffset, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderCopyBufferToTexture(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUTexelCopyBufferInfo const *source,
    WGPUTexelCopyTextureInfo const *destination, WGPUExtent3D const *copySize) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderCopyBufferToTexture(commandEncoder, source, destination,
                                        copySize);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderCopyTextureToBuffer(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUTexelCopyTextureInfo const *source,
    WGPUTexelCopyBufferInfo const *destination, WGPUExtent3D const *copySize) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderCopyTextureToBuffer(commandEncoder, source, destination,
                                        copySize);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderCopyTextureToTexture(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUTexelCopyTextureInfo const *source,
    WGPUTexelCopyTextureInfo const *destination, WGPUExtent3D const *copySize) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderCopyTextureToTexture(commandEncoder, source, destination,
                                         copySize);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUCommandBuffer go_wgpuCommandEncoderFinish(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPU_NULLABLE WGPUCommandBufferDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUCommandBuffer ret = wgpuCommandEncoderFinish(commandEncoder, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void
go_wgpuCommandEncoderInsertDebugMarker(WGPUDevice _dev, char *err,
                                       WGPUCommandEncoder commandEncoder,
                                       WGPUStringView markerLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderInsertDebugMarker(commandEncoder, markerLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandEncoderPopDebugGroup(WGPUDevice _dev, char *err,
                                   WGPUCommandEncoder commandEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderPopDebugGroup(commandEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandEncoderPushDebugGroup(WGPUDevice _dev, char *err,
                                    WGPUCommandEncoder commandEncoder,
                                    WGPUStringView groupLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderPushDebugGroup(commandEncoder, groupLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderResolveQuerySet(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUQuerySet querySet, uint32_t firstQuery, uint32_t queryCount,
    WGPUBuffer destination, uint64_t destinationOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderResolveQuerySet(commandEncoder, querySet, firstQuery,
                                    queryCount, destination, destinationOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandEncoderSetLabel(WGPUDevice _dev, char *err,
                              WGPUCommandEncoder commandEncoder,
                              WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderSetLabel(commandEncoder, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuCommandEncoderWriteTimestamp(
    WGPUDevice _dev, char *err, WGPUCommandEncoder commandEncoder,
    WGPUQuerySet querySet, uint32_t queryIndex) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderWriteTimestamp(commandEncoder, querySet, queryIndex);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandEncoderAddRef(WGPUDevice _dev, char *err,
                            WGPUCommandEncoder commandEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderAddRef(commandEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuCommandEncoderRelease(WGPUDevice _dev, char *err,
                             WGPUCommandEncoder commandEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuCommandEncoderRelease(commandEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderDispatchWorkgroups(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    uint32_t workgroupCountX, uint32_t workgroupCountY,
    uint32_t workgroupCountZ) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderDispatchWorkgroups(computePassEncoder, workgroupCountX,
                                           workgroupCountY, workgroupCountZ);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderDispatchWorkgroupsIndirect(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    WGPUBuffer indirectBuffer, uint64_t indirectOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderDispatchWorkgroupsIndirect(
      computePassEncoder, indirectBuffer, indirectOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePassEncoderEnd(WGPUDevice _dev, char *err,
                             WGPUComputePassEncoder computePassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderEnd(computePassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderInsertDebugMarker(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    WGPUStringView markerLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderInsertDebugMarker(computePassEncoder, markerLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderPopDebugGroup(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderPopDebugGroup(computePassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderPushDebugGroup(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    WGPUStringView groupLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderPushDebugGroup(computePassEncoder, groupLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderSetBindGroup(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    uint32_t groupIndex, WGPU_NULLABLE WGPUBindGroup group,
    size_t dynamicOffsetCount, uint32_t const *dynamicOffsets) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderSetBindGroup(computePassEncoder, groupIndex, group,
                                     dynamicOffsetCount, dynamicOffsets);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuComputePassEncoderSetImmediates(
    WGPUDevice _dev, char *err, WGPUComputePassEncoder computePassEncoder,
    uint32_t offset, void const *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderSetImmediates(computePassEncoder, offset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePassEncoderSetLabel(WGPUDevice _dev, char *err,
                                  WGPUComputePassEncoder computePassEncoder,
                                  WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderSetLabel(computePassEncoder, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePassEncoderSetPipeline(WGPUDevice _dev, char *err,
                                     WGPUComputePassEncoder computePassEncoder,
                                     WGPUComputePipeline pipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderSetPipeline(computePassEncoder, pipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePassEncoderAddRef(WGPUDevice _dev, char *err,
                                WGPUComputePassEncoder computePassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderAddRef(computePassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePassEncoderRelease(WGPUDevice _dev, char *err,
                                 WGPUComputePassEncoder computePassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePassEncoderRelease(computePassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUBindGroupLayout
go_wgpuComputePipelineGetBindGroupLayout(WGPUDevice _dev, char *err,
                                         WGPUComputePipeline computePipeline,
                                         uint32_t groupIndex) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBindGroupLayout ret =
      wgpuComputePipelineGetBindGroupLayout(computePipeline, groupIndex);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void
go_wgpuComputePipelineSetLabel(WGPUDevice _dev, char *err,
                               WGPUComputePipeline computePipeline,
                               WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePipelineSetLabel(computePipeline, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePipelineAddRef(WGPUDevice _dev, char *err,
                             WGPUComputePipeline computePipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePipelineAddRef(computePipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuComputePipelineRelease(WGPUDevice _dev, char *err,
                              WGPUComputePipeline computePipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuComputePipelineRelease(computePipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUBindGroup
go_wgpuDeviceCreateBindGroup(WGPUDevice _dev, char *err, WGPUDevice device,
                             WGPUBindGroupDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBindGroup ret = wgpuDeviceCreateBindGroup(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBindGroupLayout go_wgpuDeviceCreateBindGroupLayout(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPUBindGroupLayoutDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBindGroupLayout ret = wgpuDeviceCreateBindGroupLayout(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBuffer
go_wgpuDeviceCreateBuffer(WGPUDevice _dev, char *err, WGPUDevice device,
                          WGPUBufferDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBuffer ret = wgpuDeviceCreateBuffer(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUCommandEncoder go_wgpuDeviceCreateCommandEncoder(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPU_NULLABLE WGPUCommandEncoderDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUCommandEncoder ret = wgpuDeviceCreateCommandEncoder(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUComputePipeline go_wgpuDeviceCreateComputePipeline(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPUComputePipelineDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUComputePipeline ret = wgpuDeviceCreateComputePipeline(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture go_wgpuDeviceCreateComputePipelineAsync(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPUComputePipelineDescriptor const *descriptor,
    WGPUCreateComputePipelineAsyncCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret =
      wgpuDeviceCreateComputePipelineAsync(device, descriptor, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUPipelineLayout go_wgpuDeviceCreatePipelineLayout(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPUPipelineLayoutDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUPipelineLayout ret = wgpuDeviceCreatePipelineLayout(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUQuerySet
go_wgpuDeviceCreateQuerySet(WGPUDevice _dev, char *err, WGPUDevice device,
                            WGPUQuerySetDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUQuerySet ret = wgpuDeviceCreateQuerySet(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPURenderBundleEncoder go_wgpuDeviceCreateRenderBundleEncoder(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPURenderBundleEncoderDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPURenderBundleEncoder ret =
      wgpuDeviceCreateRenderBundleEncoder(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPURenderPipeline go_wgpuDeviceCreateRenderPipeline(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPURenderPipelineDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPURenderPipeline ret = wgpuDeviceCreateRenderPipeline(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture go_wgpuDeviceCreateRenderPipelineAsync(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPURenderPipelineDescriptor const *descriptor,
    WGPUCreateRenderPipelineAsyncCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret =
      wgpuDeviceCreateRenderPipelineAsync(device, descriptor, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUSampler go_wgpuDeviceCreateSampler(
    WGPUDevice _dev, char *err, WGPUDevice device,
    WGPU_NULLABLE WGPUSamplerDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUSampler ret = wgpuDeviceCreateSampler(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUShaderModule
go_wgpuDeviceCreateShaderModule(WGPUDevice _dev, char *err, WGPUDevice device,
                                WGPUShaderModuleDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUShaderModule ret = wgpuDeviceCreateShaderModule(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUTexture
go_wgpuDeviceCreateTexture(WGPUDevice _dev, char *err, WGPUDevice device,
                           WGPUTextureDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTexture ret = wgpuDeviceCreateTexture(device, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuDeviceDestroy(WGPUDevice _dev, char *err,
                                        WGPUDevice device) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDeviceDestroy(device);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus
go_wgpuDeviceGetAdapterInfo(WGPUDevice _dev, char *err, WGPUDevice device,
                            WGPUAdapterInfo *adapterInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuDeviceGetAdapterInfo(device, adapterInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuDeviceGetFeatures(WGPUDevice _dev, char *err,
                                            WGPUDevice device,
                                            WGPUSupportedFeatures *features) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDeviceGetFeatures(device, features);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus go_wgpuDeviceGetLimits(WGPUDevice _dev, char *err,
                                                WGPUDevice device,
                                                WGPULimits *limits) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuDeviceGetLimits(device, limits);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture go_wgpuDeviceGetLostFuture(WGPUDevice _dev, char *err,
                                                    WGPUDevice device) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuDeviceGetLostFuture(device);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUQueue go_wgpuDeviceGetQueue(WGPUDevice _dev, char *err,
                                              WGPUDevice device) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUQueue ret = wgpuDeviceGetQueue(device);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUBool go_wgpuDeviceHasFeature(WGPUDevice _dev, char *err,
                                               WGPUDevice device,
                                               WGPUFeatureName feature) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBool ret = wgpuDeviceHasFeature(device, feature);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUFuture
go_wgpuDevicePopErrorScope(WGPUDevice _dev, char *err, WGPUDevice device,
                           WGPUPopErrorScopeCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuDevicePopErrorScope(device, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuDevicePushErrorScope(WGPUDevice _dev, char *err,
                                               WGPUDevice device,
                                               WGPUErrorFilter filter) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDevicePushErrorScope(device, filter);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuDeviceSetLabel(WGPUDevice _dev, char *err,
                                         WGPUDevice device,
                                         WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDeviceSetLabel(device, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuDeviceAddRef(WGPUDevice _dev, char *err,
                                       WGPUDevice device) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDeviceAddRef(device);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuDeviceRelease(WGPUDevice _dev, char *err,
                                        WGPUDevice device) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuDeviceRelease(device);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuExternalTextureSetLabel(WGPUDevice _dev, char *err,
                               WGPUExternalTexture externalTexture,
                               WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuExternalTextureSetLabel(externalTexture, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuExternalTextureAddRef(WGPUDevice _dev, char *err,
                             WGPUExternalTexture externalTexture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuExternalTextureAddRef(externalTexture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuExternalTextureRelease(WGPUDevice _dev, char *err,
                              WGPUExternalTexture externalTexture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuExternalTextureRelease(externalTexture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUSurface
go_wgpuInstanceCreateSurface(WGPUDevice _dev, char *err, WGPUInstance instance,
                             WGPUSurfaceDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUSurface ret = wgpuInstanceCreateSurface(instance, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuInstanceGetWGSLLanguageFeatures(
    WGPUDevice _dev, char *err, WGPUInstance instance,
    WGPUSupportedWGSLLanguageFeatures *features) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuInstanceGetWGSLLanguageFeatures(instance, features);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUBool
go_wgpuInstanceHasWGSLLanguageFeature(WGPUDevice _dev, char *err,
                                      WGPUInstance instance,
                                      WGPUWGSLLanguageFeatureName feature) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBool ret = wgpuInstanceHasWGSLLanguageFeature(instance, feature);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuInstanceProcessEvents(WGPUDevice _dev, char *err,
                                                WGPUInstance instance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuInstanceProcessEvents(instance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUFuture go_wgpuInstanceRequestAdapter(
    WGPUDevice _dev, char *err, WGPUInstance instance,
    WGPU_NULLABLE WGPURequestAdapterOptions const *options,
    WGPURequestAdapterCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuInstanceRequestAdapter(instance, options, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUWaitStatus go_wgpuInstanceWaitAny(
    WGPUDevice _dev, char *err, WGPUInstance instance, size_t futureCount,
    WGPU_NULLABLE WGPUFutureWaitInfo *futures, uint64_t timeoutNS) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUWaitStatus ret =
      wgpuInstanceWaitAny(instance, futureCount, futures, timeoutNS);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuInstanceAddRef(WGPUDevice _dev, char *err,
                                         WGPUInstance instance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuInstanceAddRef(instance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuInstanceRelease(WGPUDevice _dev, char *err,
                                          WGPUInstance instance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuInstanceRelease(instance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuPipelineLayoutSetLabel(WGPUDevice _dev, char *err,
                              WGPUPipelineLayout pipelineLayout,
                              WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuPipelineLayoutSetLabel(pipelineLayout, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuPipelineLayoutAddRef(WGPUDevice _dev, char *err,
                            WGPUPipelineLayout pipelineLayout) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuPipelineLayoutAddRef(pipelineLayout);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuPipelineLayoutRelease(WGPUDevice _dev, char *err,
                             WGPUPipelineLayout pipelineLayout) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuPipelineLayoutRelease(pipelineLayout);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQuerySetDestroy(WGPUDevice _dev, char *err,
                                          WGPUQuerySet querySet) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQuerySetDestroy(querySet);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline uint32_t go_wgpuQuerySetGetCount(WGPUDevice _dev, char *err,
                                               WGPUQuerySet querySet) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuQuerySetGetCount(querySet);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUQueryType go_wgpuQuerySetGetType(WGPUDevice _dev, char *err,
                                                   WGPUQuerySet querySet) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUQueryType ret = wgpuQuerySetGetType(querySet);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuQuerySetSetLabel(WGPUDevice _dev, char *err,
                                           WGPUQuerySet querySet,
                                           WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQuerySetSetLabel(querySet, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQuerySetAddRef(WGPUDevice _dev, char *err,
                                         WGPUQuerySet querySet) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQuerySetAddRef(querySet);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQuerySetRelease(WGPUDevice _dev, char *err,
                                          WGPUQuerySet querySet) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQuerySetRelease(querySet);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUFuture
go_wgpuQueueOnSubmittedWorkDone(WGPUDevice _dev, char *err, WGPUQueue queue,
                                WGPUQueueWorkDoneCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret = wgpuQueueOnSubmittedWorkDone(queue, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuQueueSetLabel(WGPUDevice _dev, char *err,
                                        WGPUQueue queue, WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueSetLabel(queue, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQueueSubmit(WGPUDevice _dev, char *err,
                                      WGPUQueue queue, size_t commandCount,
                                      WGPUCommandBuffer const *commands) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueSubmit(queue, commandCount, commands);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQueueWriteBuffer(WGPUDevice _dev, char *err,
                                           WGPUQueue queue, WGPUBuffer buffer,
                                           uint64_t bufferOffset,
                                           void const *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueWriteBuffer(queue, buffer, bufferOffset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuQueueWriteTexture(WGPUDevice _dev, char *err, WGPUQueue queue,
                         WGPUTexelCopyTextureInfo const *destination,
                         void const *data, size_t dataSize,
                         WGPUTexelCopyBufferLayout const *dataLayout,
                         WGPUExtent3D const *writeSize) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueWriteTexture(queue, destination, data, dataSize, dataLayout,
                        writeSize);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQueueAddRef(WGPUDevice _dev, char *err,
                                      WGPUQueue queue) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueAddRef(queue);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuQueueRelease(WGPUDevice _dev, char *err,
                                       WGPUQueue queue) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuQueueRelease(queue);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleSetLabel(WGPUDevice _dev, char *err,
                                               WGPURenderBundle renderBundle,
                                               WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleSetLabel(renderBundle, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleAddRef(WGPUDevice _dev, char *err,
                                             WGPURenderBundle renderBundle) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleAddRef(renderBundle);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleRelease(WGPUDevice _dev, char *err,
                                              WGPURenderBundle renderBundle) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleRelease(renderBundle);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderBundleEncoderDraw(WGPUDevice _dev, char *err,
                               WGPURenderBundleEncoder renderBundleEncoder,
                               uint32_t vertexCount, uint32_t instanceCount,
                               uint32_t firstVertex, uint32_t firstInstance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderDraw(renderBundleEncoder, vertexCount, instanceCount,
                              firstVertex, firstInstance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderDrawIndexed(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    uint32_t indexCount, uint32_t instanceCount, uint32_t firstIndex,
    int32_t baseVertex, uint32_t firstInstance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderDrawIndexed(renderBundleEncoder, indexCount,
                                     instanceCount, firstIndex, baseVertex,
                                     firstInstance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderDrawIndexedIndirect(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPUBuffer indirectBuffer, uint64_t indirectOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderDrawIndexedIndirect(renderBundleEncoder,
                                             indirectBuffer, indirectOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderDrawIndirect(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPUBuffer indirectBuffer, uint64_t indirectOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderDrawIndirect(renderBundleEncoder, indirectBuffer,
                                      indirectOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPURenderBundle go_wgpuRenderBundleEncoderFinish(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPU_NULLABLE WGPURenderBundleDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPURenderBundle ret =
      wgpuRenderBundleEncoderFinish(renderBundleEncoder, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuRenderBundleEncoderInsertDebugMarker(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPUStringView markerLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderInsertDebugMarker(renderBundleEncoder, markerLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderPopDebugGroup(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderPopDebugGroup(renderBundleEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderPushDebugGroup(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPUStringView groupLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderPushDebugGroup(renderBundleEncoder, groupLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderSetBindGroup(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    uint32_t groupIndex, WGPU_NULLABLE WGPUBindGroup group,
    size_t dynamicOffsetCount, uint32_t const *dynamicOffsets) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetBindGroup(renderBundleEncoder, groupIndex, group,
                                      dynamicOffsetCount, dynamicOffsets);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderSetImmediates(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    uint32_t offset, void const *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetImmediates(renderBundleEncoder, offset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderSetIndexBuffer(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPUBuffer buffer, WGPUIndexFormat format, uint64_t offset, uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetIndexBuffer(renderBundleEncoder, buffer, format,
                                        offset, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderBundleEncoderSetLabel(WGPUDevice _dev, char *err,
                                   WGPURenderBundleEncoder renderBundleEncoder,
                                   WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetLabel(renderBundleEncoder, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderSetPipeline(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    WGPURenderPipeline pipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetPipeline(renderBundleEncoder, pipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderBundleEncoderSetVertexBuffer(
    WGPUDevice _dev, char *err, WGPURenderBundleEncoder renderBundleEncoder,
    uint32_t slot, WGPU_NULLABLE WGPUBuffer buffer, uint64_t offset,
    uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderSetVertexBuffer(renderBundleEncoder, slot, buffer,
                                         offset, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderBundleEncoderAddRef(WGPUDevice _dev, char *err,
                                 WGPURenderBundleEncoder renderBundleEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderAddRef(renderBundleEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderBundleEncoderRelease(WGPUDevice _dev, char *err,
                                  WGPURenderBundleEncoder renderBundleEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderBundleEncoderRelease(renderBundleEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderBeginOcclusionQuery(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t queryIndex) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderBeginOcclusionQuery(renderPassEncoder, queryIndex);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderDraw(WGPUDevice _dev, char *err,
                             WGPURenderPassEncoder renderPassEncoder,
                             uint32_t vertexCount, uint32_t instanceCount,
                             uint32_t firstVertex, uint32_t firstInstance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderDraw(renderPassEncoder, vertexCount, instanceCount,
                            firstVertex, firstInstance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderDrawIndexed(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t indexCount, uint32_t instanceCount, uint32_t firstIndex,
    int32_t baseVertex, uint32_t firstInstance) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderDrawIndexed(renderPassEncoder, indexCount, instanceCount,
                                   firstIndex, baseVertex, firstInstance);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderDrawIndexedIndirect(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    WGPUBuffer indirectBuffer, uint64_t indirectOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderDrawIndexedIndirect(renderPassEncoder, indirectBuffer,
                                           indirectOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderDrawIndirect(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    WGPUBuffer indirectBuffer, uint64_t indirectOffset) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderDrawIndirect(renderPassEncoder, indirectBuffer,
                                    indirectOffset);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderEnd(WGPUDevice _dev, char *err,
                            WGPURenderPassEncoder renderPassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderEnd(renderPassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderEndOcclusionQuery(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderEndOcclusionQuery(renderPassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderExecuteBundles(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    size_t bundleCount, WGPURenderBundle const *bundles) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderExecuteBundles(renderPassEncoder, bundleCount, bundles);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderInsertDebugMarker(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    WGPUStringView markerLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderInsertDebugMarker(renderPassEncoder, markerLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderPopDebugGroup(WGPUDevice _dev, char *err,
                                      WGPURenderPassEncoder renderPassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderPopDebugGroup(renderPassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderPushDebugGroup(WGPUDevice _dev, char *err,
                                       WGPURenderPassEncoder renderPassEncoder,
                                       WGPUStringView groupLabel) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderPushDebugGroup(renderPassEncoder, groupLabel);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetBindGroup(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t groupIndex, WGPU_NULLABLE WGPUBindGroup group,
    size_t dynamicOffsetCount, uint32_t const *dynamicOffsets) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetBindGroup(renderPassEncoder, groupIndex, group,
                                    dynamicOffsetCount, dynamicOffsets);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetBlendConstant(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    WGPUColor const *color) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetBlendConstant(renderPassEncoder, color);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetImmediates(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t offset, void const *data, size_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetImmediates(renderPassEncoder, offset, data, size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetIndexBuffer(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    WGPUBuffer buffer, WGPUIndexFormat format, uint64_t offset, uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetIndexBuffer(renderPassEncoder, buffer, format, offset,
                                      size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderSetLabel(WGPUDevice _dev, char *err,
                                 WGPURenderPassEncoder renderPassEncoder,
                                 WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetLabel(renderPassEncoder, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderSetPipeline(WGPUDevice _dev, char *err,
                                    WGPURenderPassEncoder renderPassEncoder,
                                    WGPURenderPipeline pipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetPipeline(renderPassEncoder, pipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetScissorRect(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t x, uint32_t y, uint32_t width, uint32_t height) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetScissorRect(renderPassEncoder, x, y, width, height);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetStencilReference(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t reference) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetStencilReference(renderPassEncoder, reference);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuRenderPassEncoderSetVertexBuffer(
    WGPUDevice _dev, char *err, WGPURenderPassEncoder renderPassEncoder,
    uint32_t slot, WGPU_NULLABLE WGPUBuffer buffer, uint64_t offset,
    uint64_t size) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetVertexBuffer(renderPassEncoder, slot, buffer, offset,
                                       size);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderSetViewport(WGPUDevice _dev, char *err,
                                    WGPURenderPassEncoder renderPassEncoder,
                                    float x, float y, float width, float height,
                                    float minDepth, float maxDepth) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderSetViewport(renderPassEncoder, x, y, width, height,
                                   minDepth, maxDepth);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderAddRef(WGPUDevice _dev, char *err,
                               WGPURenderPassEncoder renderPassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderAddRef(renderPassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPassEncoderRelease(WGPUDevice _dev, char *err,
                                WGPURenderPassEncoder renderPassEncoder) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPassEncoderRelease(renderPassEncoder);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUBindGroupLayout
go_wgpuRenderPipelineGetBindGroupLayout(WGPUDevice _dev, char *err,
                                        WGPURenderPipeline renderPipeline,
                                        uint32_t groupIndex) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUBindGroupLayout ret =
      wgpuRenderPipelineGetBindGroupLayout(renderPipeline, groupIndex);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void
go_wgpuRenderPipelineSetLabel(WGPUDevice _dev, char *err,
                              WGPURenderPipeline renderPipeline,
                              WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPipelineSetLabel(renderPipeline, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPipelineAddRef(WGPUDevice _dev, char *err,
                            WGPURenderPipeline renderPipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPipelineAddRef(renderPipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuRenderPipelineRelease(WGPUDevice _dev, char *err,
                             WGPURenderPipeline renderPipeline) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuRenderPipelineRelease(renderPipeline);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSamplerSetLabel(WGPUDevice _dev, char *err,
                                          WGPUSampler sampler,
                                          WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSamplerSetLabel(sampler, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSamplerAddRef(WGPUDevice _dev, char *err,
                                        WGPUSampler sampler) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSamplerAddRef(sampler);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSamplerRelease(WGPUDevice _dev, char *err,
                                         WGPUSampler sampler) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSamplerRelease(sampler);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUFuture go_wgpuShaderModuleGetCompilationInfo(
    WGPUDevice _dev, char *err, WGPUShaderModule shaderModule,
    WGPUCompilationInfoCallbackInfo callbackInfo) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUFuture ret =
      wgpuShaderModuleGetCompilationInfo(shaderModule, callbackInfo);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuShaderModuleSetLabel(WGPUDevice _dev, char *err,
                                               WGPUShaderModule shaderModule,
                                               WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuShaderModuleSetLabel(shaderModule, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuShaderModuleAddRef(WGPUDevice _dev, char *err,
                                             WGPUShaderModule shaderModule) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuShaderModuleAddRef(shaderModule);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuShaderModuleRelease(WGPUDevice _dev, char *err,
                                              WGPUShaderModule shaderModule) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuShaderModuleRelease(shaderModule);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuSupportedFeaturesFreeMembers(WGPUDevice _dev, char *err,
                                    WGPUSupportedFeatures supportedFeatures) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSupportedFeaturesFreeMembers(supportedFeatures);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSupportedInstanceFeaturesFreeMembers(
    WGPUDevice _dev, char *err,
    WGPUSupportedInstanceFeatures supportedInstanceFeatures) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSupportedInstanceFeaturesFreeMembers(supportedInstanceFeatures);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSupportedWGSLLanguageFeaturesFreeMembers(
    WGPUDevice _dev, char *err,
    WGPUSupportedWGSLLanguageFeatures supportedWGSLLanguageFeatures) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSupportedWGSLLanguageFeaturesFreeMembers(supportedWGSLLanguageFeatures);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void
go_wgpuSurfaceConfigure(WGPUDevice _dev, char *err, WGPUSurface surface,
                        WGPUSurfaceConfiguration const *config) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceConfigure(surface, config);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus
go_wgpuSurfaceGetCapabilities(WGPUDevice _dev, char *err, WGPUSurface surface,
                              WGPUAdapter adapter,
                              WGPUSurfaceCapabilities *capabilities) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuSurfaceGetCapabilities(surface, adapter, capabilities);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void
go_wgpuSurfaceGetCurrentTexture(WGPUDevice _dev, char *err, WGPUSurface surface,
                                WGPUSurfaceTexture *surfaceTexture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceGetCurrentTexture(surface, surfaceTexture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUStatus go_wgpuSurfacePresent(WGPUDevice _dev, char *err,
                                               WGPUSurface surface) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUStatus ret = wgpuSurfacePresent(surface);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuSurfaceSetLabel(WGPUDevice _dev, char *err,
                                          WGPUSurface surface,
                                          WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceSetLabel(surface, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSurfaceUnconfigure(WGPUDevice _dev, char *err,
                                             WGPUSurface surface) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceUnconfigure(surface);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSurfaceAddRef(WGPUDevice _dev, char *err,
                                        WGPUSurface surface) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceAddRef(surface);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSurfaceRelease(WGPUDevice _dev, char *err,
                                         WGPUSurface surface) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceRelease(surface);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuSurfaceCapabilitiesFreeMembers(
    WGPUDevice _dev, char *err, WGPUSurfaceCapabilities surfaceCapabilities) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuSurfaceCapabilitiesFreeMembers(surfaceCapabilities);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline WGPUTextureView go_wgpuTextureCreateView(
    WGPUDevice _dev, char *err, WGPUTexture texture,
    WGPU_NULLABLE WGPUTextureViewDescriptor const *descriptor) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTextureView ret = wgpuTextureCreateView(texture, descriptor);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuTextureDestroy(WGPUDevice _dev, char *err,
                                         WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureDestroy(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline uint32_t
go_wgpuTextureGetDepthOrArrayLayers(WGPUDevice _dev, char *err,
                                    WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuTextureGetDepthOrArrayLayers(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUTextureDimension
go_wgpuTextureGetDimension(WGPUDevice _dev, char *err, WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTextureDimension ret = wgpuTextureGetDimension(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUTextureFormat
go_wgpuTextureGetFormat(WGPUDevice _dev, char *err, WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTextureFormat ret = wgpuTextureGetFormat(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline uint32_t go_wgpuTextureGetHeight(WGPUDevice _dev, char *err,
                                               WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuTextureGetHeight(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline uint32_t go_wgpuTextureGetMipLevelCount(WGPUDevice _dev,
                                                      char *err,
                                                      WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuTextureGetMipLevelCount(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline uint32_t go_wgpuTextureGetSampleCount(WGPUDevice _dev, char *err,
                                                    WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuTextureGetSampleCount(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUTextureViewDimension
go_wgpuTextureGetTextureBindingViewDimension(WGPUDevice _dev, char *err,
                                             WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTextureViewDimension ret =
      wgpuTextureGetTextureBindingViewDimension(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline WGPUTextureUsage
go_wgpuTextureGetUsage(WGPUDevice _dev, char *err, WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  WGPUTextureUsage ret = wgpuTextureGetUsage(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline uint32_t go_wgpuTextureGetWidth(WGPUDevice _dev, char *err,
                                              WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  uint32_t ret = wgpuTextureGetWidth(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
  return ret;
}

static inline void go_wgpuTextureSetLabel(WGPUDevice _dev, char *err,
                                          WGPUTexture texture,
                                          WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureSetLabel(texture, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuTextureAddRef(WGPUDevice _dev, char *err,
                                        WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureAddRef(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuTextureRelease(WGPUDevice _dev, char *err,
                                         WGPUTexture texture) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureRelease(texture);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuTextureViewSetLabel(WGPUDevice _dev, char *err,
                                              WGPUTextureView textureView,
                                              WGPUStringView label) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureViewSetLabel(textureView, label);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuTextureViewAddRef(WGPUDevice _dev, char *err,
                                            WGPUTextureView textureView) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureViewAddRef(textureView);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}

static inline void go_wgpuTextureViewRelease(WGPUDevice _dev, char *err,
                                             WGPUTextureView textureView) {
  wgpuDevicePushErrorScope(_dev, WGPUErrorFilter_Validation);
  wgpuTextureViewRelease(textureView);

  WGPUPopErrorScopeCallbackInfo const err_cb = {
      .callback = webgpu_error_callback,
      .userdata1 = (void *)err,
  };

  wgpuDevicePopErrorScope(_dev, err_cb);
}
